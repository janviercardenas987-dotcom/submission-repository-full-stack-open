```sequenceDiagram
participant browser
participant server

    Note right of browser: El usuario escribe en el input y hace clic en "Save"
    Note right of browser: El JS captura el evento, añade la nota a la lista local y redraws/renderiza las notas en el DOM

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: {"message":"note created"}
    desactive server